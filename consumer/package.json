{
  "name": "alphadrip-consumer",
  "version": "0.2.0",
  "type": "module",
  "description": "AlphaDrip subscriber — direct EIP-3009 on Arc Testnet",
  "scripts": {
    "subscribe": "tsx --env-file=.env subscribe.ts",
    "balance": "tsx --env-file=.env balance.ts"
  },
  "dependencies": {
    "viem": "^2.21.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}